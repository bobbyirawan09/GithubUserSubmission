<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bobby.irawan.favoriteconsumer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="bobby.irawan.githubuser.READ_DATABASE"
        android:protectionLevel="normal" />
    <uses-permission
        android:name="bobby.irawan.githubuser.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:name=".AppFavoriteController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="bobby.irawan.githubuser.presentation.favorite.FavoriteConsumerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>